## Recomendação: Corrigir Command Injection

### Prioridade: 🔴 Crítica

### Ação Imediata (< 24h)
1. Desabilitar comando `ping` no VulnService
2. Restringir acesso via firewall (whitelist de IPs)
3. Monitorar logs para tentativas de exploração

### Correção Permanente (< 7 dias)
1. Implementar validação de entrada:
```python
import re
if not re.match(r'^[a-zA-Z0-9.-]+$', host):
return "Invalid input"
```
2. Usar subprocess com lista (não shell=True):
```python
subprocess.run(['ping', '-c', '1', host])
```

3. Implementar rate limiting (max 10 req/min)
4. Adicionar logging detalhado
5. Implementar WAF ou IPS

### Validação
1. Testar com payloads maliciosos
2. Code review do módulo de ping
3. Pentest de regressão

### Recursos Necessários
- 2h de desenvolvimento
- 1h de testes
- Não requer downtime

### Custo vs Risco
- Custo de implementação: Baixo (2-3h)
- Risco de não corrigir: Crítico (comprometimento total)
- ROI: Muito alto
